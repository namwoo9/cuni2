<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sejong.namu.dao.MemberDao">
	<select id="getMatchedOne" parameterType="Map"
		resultMap="Member">
		SELECT *
		FROM member
		WHERE loginId = #{loginId}
		AND loginPw =
		#{loginPw}
		AND delStatus = 0
	</select>

	<select id="getList" parameterType="Map" resultType="Member">
		SELECT *
		FROM member ORDER BY id DESC
	</select>

	<select id="getOne" parameterType="long" resultMap="Member">
		SELECT *
		FROM
		member
		WHERE id = #{loginedMemberId}
	</select>

	<select id="searchId" parameterType="Map" resultMap="Member">
		SELECT *
		FROM
		member
		WHERE name = #{name}
		AND email = #{email}
		AND delStatus = 0
	</select>

	<select id="searchPw" parameterType="Map" resultMap="Member">
		SELECT *
		FROM
		member
		WHERE name = #{name}
		AND loginId = #{loginId}
		AND email =
		#{email}
		AND delStatus = 0
	</select>

	<select id="doubleCheck" parameterType="map"
		resultType="Integer">
		SELECT COUNT(*) FROM `member`
		WHERE loginId = #{loginId} OR
		email =
		#{email} OR name = #{name}
	</select>

	<insert id="addMember" parameterType="map">
		INSERT INTO `member`
		SET
		regDate = NOW(),
		loginId = #{loginId},
		loginPw = #{loginPw},
		email =
		#{email},
		`name` = #{name},
		emailAuthKey = #{authKey}
	</insert>
	<!-- &amp; name != '' -->
	<update id="update" parameterType="map">
		UPDATE member
		<set>
			<if test="name != null">name = #{name},</if>
			<if test="email != null">email = #{email},</if>
			<if test="temporaryPassword != null">loginPw = #{temporaryPassword}</if>
			<if test="afterPw != null">loginPw = #{afterPw}</if>
		</set>
		WHERE id = #{id}
	</update>

	<select id="getEmailMember" parameterType="map"
		resultType="Member">
		SELECT * FROM `member`
		WHERE email = #{email}
		AND
		emailAuthKey =
		#{authKey}
	</select>
	<select id="checkOverId" parameterType="String" resultType="int">
		SELECT COUNT(*) FROM `member` WHERE loginId = #{loginId}
	</select>

	<select id="checkOverName" parameterType="String"
		resultType="int">
		SELECT COUNT(*) FROM `member` WHERE name = #{name}
	</select>


	<update id="updateAuthStatus" parameterType="map">
		UPDATE `member`
		SET
		emailAuthStatus = true
		WHERE email = #{email}
	</update>

	<update id="updateDelStatus" parameterType="map">
		UPDATE `member`
		SET
		delStatus = 1
		WHERE id = #{id}
	</update>
	
	<update id="stop" parameterType="int">
		UPDATE member
		SET stop = 1
		WHERE id = #{id}
	</update>
	
	<update id="stopCancel" parameterType="int">
		UPDATE member
		SET `stop` = 0
		WHERE id = #{id};
	</update>

	<resultMap type="Member" id="Member">
		<id property="id" column="id" />
		<id property="regDate" column="regDate" />
		<id property="loginId" column="loginId" />
		<id property="loginPw" column="loginPw" />
		<id property="name" column="name" />
		<id property="emailAuthKey" column="emailAuthKey" />
		<id property="emailAuthStatus" column="emailAuthStatus" />
		<id property="delStatus" column="delStatus" />
	</resultMap>
</mapper>